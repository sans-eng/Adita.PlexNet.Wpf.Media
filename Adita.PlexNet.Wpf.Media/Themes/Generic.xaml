<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Adita.PlexNet.Wpf.Controls">
    
    <!--<Style TargetType="{x:Type local:MaterialIcon}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MaterialIcon}">
                    <Glyphs 
                        FontRenderingEmSize="{TemplateBinding Size}"
                        StyleSimulations="None"
                        UnicodeString=""
                        Fill="{TemplateBinding Brush}"
                        OriginX="0"
                        OriginY="{TemplateBinding Size}"
                        />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    
</ResourceDictionary>
